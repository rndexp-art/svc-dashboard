[project]
name = "rndexpart-dashboard"
version = "0.1.0"
description = "Ecosystem dashboard for rndexp.art — admin UI gated by svc-auth."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "httpx>=0.27",
  "jinja2>=3.1",
  "python-multipart>=0.0.12",
]

[tool.uv]
package = false
